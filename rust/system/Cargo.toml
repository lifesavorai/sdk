[package]
name = "lifesavor-system-sdk"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
license = "MIT"
description = "Life Savor System SDK — build system components using the SystemComponent trait"
repository = "https://github.com/lifesavor/lifesavor-agent"
homepage = "https://developer.lifesavor.ai"
keywords = ["lifesavor", "sdk", "system-component", "agent", "plugin"]
categories = ["development-tools", "api-bindings"]
readme = "README.md"

[features]
default = []
analytics = []

[dependencies]
# For crates.io publishing, replace the path dependency with a version dependency:
#   lifesavor-agent-types = "0.1"
# See ../PUBLISHING.md for the full publishing workflow.
#
# Path dependency resolves correctly both in local development AND when this
# crate is consumed as a git dependency (cargo resolves paths relative to the
# git checkout root).
lifesavor-agent-types = { path = "../agent" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
tracing = "0.1"
tokio = { version = "1.36", features = ["full"] }
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.7", features = ["v4", "serde"] }
async-trait = "0.1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.10"
proptest = "1.4"
tracing-subscriber = { version = "0.3", features = ["fmt"] }
